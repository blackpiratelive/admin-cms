import { PlaceholderModule } from "@/components/PlaceholderModule";

export default function BlogPlaceholderPage() {
  return (
    <PlaceholderModule
      title="Long-form Blog Posts"
      description="Manage long-form articles, post drafts, SEO metadata, and canonical URLs for Hugo website."
      category="Content"
    />
  );
}
