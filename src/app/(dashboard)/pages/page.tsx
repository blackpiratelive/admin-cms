import { PlaceholderModule } from "@/components/PlaceholderModule";

export default function PagesPlaceholderPage() {
  return (
    <PlaceholderModule
      title="Static Site Pages"
      description="Static landing pages, about page, resume, and contact information."
      category="Content"
    />
  );
}
