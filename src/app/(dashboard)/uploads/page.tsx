import { PlaceholderModule } from "@/components/PlaceholderModule";

export default function UploadsPlaceholderPage() {
  return (
    <PlaceholderModule
      title="File & Media Uploads"
      description="Manage raw file assets, PDFs, and external cloud storage references."
      category="Media"
    />
  );
}
